import {StyleSheet} from "react-native";

export const globalFontSize = 20

export const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#457945',
        alignItems: 'center',
        justifyContent: 'center',
    },
    border: {
        borderStyle: "solid",
        borderWidth: 2,
        borderColor: 'blue'
    },

})

export const colors= {
  white: '#f5f0f0',
  grey: '#373f3d',
  lightGrey: '#7ca491'
}